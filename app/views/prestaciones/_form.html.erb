<%= form_for(@prestacion) do |f| %>
  <% if @prestacion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@prestacion.errors.count, "error") %> prohibited this prestacion from being saved:</h2>

      <ul>
      <% @prestacion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cod_prestacion %><br />
    <%= f.text_field :cod_prestacion %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br />
    <%= f.text_field :descripcion %>
  </div>
  <div class="field">
    <%= f.label :descripcion %><br />
    <%= f.collection_select(:punto_servicio_id, PuntoServicio.all, :id, :descripcion, {:include_blank => true}) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
