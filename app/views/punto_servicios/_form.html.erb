<%= form_for(@punto_servicio) do |f| %>
  <% if @punto_servicio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@punto_servicio.errors.count, "error") %> prohibited this punto_servicio from being saved:</h2>

      <ul>
      <% @punto_servicio.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %><br />
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
