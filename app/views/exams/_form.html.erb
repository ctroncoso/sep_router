<%= form_for(@exam) do |f| %>
  <% if @exam.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exam.errors.count, "error") %> prohibited this exam from being saved:</h2>

      <ul>
      <% @exam.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :patient_id %><br />
    <%= f.number_field :patient_id %>
  </div>
  <div class="field">
    <%= f.label :prestacion_id %><br />
    <%= f.number_field :prestacion_id %>
  </div>
  <div class="field">
    <%= f.label :exam_name %><br />
    <%= f.text_area :exam_name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
